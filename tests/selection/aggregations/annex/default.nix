# A third system-scope aggregation that names the SAME provider-bearing member
# as workstation and agrees with it. Two aggregations wanting the same dendrite
# on the same terms must merge into one selection, not fight.
{
  description = "Select the annex aggregation.";

  system.providers.notifications = "dunst";
}
