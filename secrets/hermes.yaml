# PLAINTEXT — encrypt before committing:
#   sops -e -i secrets/hermes.yaml
#
# To create your age key (once):
#   mkdir -p ~/.config/sops/age
#   age-keygen -o ~/.config/sops/age/keys.txt
#   # paste the public key (age1...) into .sops.yaml replacing age1REPLACEME
hermes/api-key: "PASTE_API_KEY_HERE"
