# A home-scope aggregation. The same body answers in both scopes and this one
# declares only a home half, so selecting it from a user contributes home
# members and nothing at system scope.
{
  description = "Select the desk aggregation.";

  home.providers.notifications = "dunst";
}
