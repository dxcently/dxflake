# PLAINTEXT PLACEHOLDER — encrypt before it means anything:
#   1. Create a reusable, non-ephemeral auth key in the Tailscale admin console.
#   2. Put it below, then encrypt in place:  sops -e -i secrets/tailscale.yaml
#
# This must be encrypted to every host that joins the mesh. For each host add its
# ssh-to-age pubkey to .sops.yaml (see that file), then: sops updatekeys secrets/tailscale.yaml
tailscale/authkey: "PASTE_TAILSCALE_AUTHKEY_HERE"
