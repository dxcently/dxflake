# Claude Code — ignore all artifacts, keep CLAUDE.md (repo root, not under .claude/)
.claude/
CLAUDE.local.md
tasks/

# Obsidian
.obsidian/
.trash/
*.base

# Editor / OS cruft
.direnv/
result
result-*
*.swp
.DS_Store

# sops — decrypted plaintext must NEVER be committed.
# The encrypted secrets/*.yaml stay tracked; these are the `sops -d` outputs.
*.dec
*.dec.yaml
*.dec.json
*.decrypted
*.plain.yaml
secrets/*.dec.*
